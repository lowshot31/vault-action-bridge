{
  "name": "note-pilot",
  "version": "0.1.7",
  "description": "AI-powered Obsidian vault assistant for Q&A and file operations with human-in-the-loop approval.",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "release:check": "node scripts/release-check.js",
    "verify": "npm test && npm run release:check"
  }
}